[project]
name = "ai-service-platform-internal-tooling"
version = "0.1.0"
description = "Internal operator-facing tooling for Phase 1 service delivery"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build-meta"

[project.scripts]
asp-tool = "ai_service_platform.main:main"
